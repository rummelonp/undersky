<div class="comment">
  <span class="username" data-username="<%= comment.from.username %>">
    <%= link_to comment.from.username, recent_url(id: comment.from.id) %>
  </span>
  <span class="text">
    <%= emoji_tag tags_tag comment.text %>
  </span>
  <span class="created_time">
    <%= Time.at(comment.created_time.to_i).to_pretty %>
  </span>
  <% if authenticated? && (mine?(comment.from.id) || (photo && mine?(photo.user.id))) %>
  <span class="comments-button delete-comment">
    <a href="<%= delete_comment_url(id: comment.from.id, comment_id: comment.id) %>" class="delete" data-remote="true" data-method="delete" data-confirm="Are you sure want to delete this comment?">×</a>
  </span>
  <% end %>
</div>
